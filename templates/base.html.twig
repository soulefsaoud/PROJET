
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Compose & Enjoy{% endblock %}</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    {# Bootstrap CSS #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">

    {# Styles personnalisés #}
    {# Import de la police Poppins pour correspondre à la charte #}
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    {# Styles personnalisés selon la charte Compose & Enjoy #}
    <style>
        :root {
            --primary-green: #2D6A41;
            --accent-orange: #FF9F1C;
            --light-gray: #F8F8F8;
            --white: #FFFFFF;
            --dark-text: #333333;
        }

        body {
            background-color: var(--light-gray);
            min-height: 100vh;
            font-family: 'Poppins', sans-serif;
            color: var(--dark-text);
            line-height: 1.6;
        }

        .card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(45, 106, 65, 0.1);
            background: var(--white);
        }

        .card-header {
            background: var(--primary-green);
            color: white;
            border-radius: 15px 15px 0 0 !important;
            border: none;
            padding: 1.5rem;
        }

        .card-header h3 {
            margin: 0;
            font-weight: 600;
        }

        .form-control {
            border: 2px solid #e9ecef;
            border-radius: 30px;
            padding: 15px 20px;
            transition: all 0.3s ease;
            font-family: 'Poppins', sans-serif;
        }

        .form-control:focus {
            border-color: var(--primary-green);
            box-shadow: 0 0 0 0.2rem rgba(45, 106, 65, 0.25);
        }

        .form-label {
            font-weight: 500;
            color: var(--primary-green);
            margin-bottom: 8px;
        }

        .btn-primary {
            background-color: var(--accent-orange);
            border: none;
            padding: 15px 30px;
            font-weight: 500;
            border-radius: 30px;
            transition: all 0.3s ease;
            font-family: 'Poppins', sans-serif;
        }

        .btn-primary:hover {
            background-color: #e88e00;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(255, 159, 28, 0.4);
        }

        .btn-outline-secondary {
            border-color: #ddd;
            color: var(--primary-green);
            border-radius: 0 30px 30px 0;
        }

        .btn-outline-secondary:hover {
            background-color: var(--primary-green);
            border-color: var(--primary-green);
        }

        .alert-danger {
            border: none;
            border-radius: 10px;
            background-color: #f8d7da;
            color: #721c24;
            border-left: 4px solid #dc3545;
        }

        a {
            color: var(--primary-green);
            text-decoration: none;
            transition: color 0.3s ease;
        }

        a:hover {
            color: var(--accent-orange);
            text-decoration: underline;
        }

        .form-check-input:checked {
            background-color: var(--primary-green);
            border-color: var(--primary-green);
        }

        .form-check-input:focus {
            border-color: var(--primary-green);
            box-shadow: 0 0 0 0.25rem rgba(45, 106, 65, 0.25);
        }

        .container {
            padding-top: 3rem;
        }

        hr {
            border-color: rgba(45, 106, 65, 0.2);
        }

        .text-muted {
            color: #666 !important;
        }

        .fw-bold {
            font-weight: 600 !important;
        }


        nav ul {
            display: flex;
            list-style: none;
            margin: 0;
            padding: 0;
            gap: 1.5rem;
            align-items: center;
        }

        nav ul li a {
            color: var(--dark-text);
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s ease;
            color: var(--primary-green);
        }

        nav ul li a:hover {
            color: var(--accent-orange);
        }


        /* Style pour le garde-manger - minimal et cohérent */
        .pantry-link {
            background-color: var(--primary-green);
            color: white !important;
            padding: 8px 15px;
            border-radius: 20px;
            font-weight: 500;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 5px;
        }

        .pantry-link:hover {
            background-color: var(--accent-orange);
            color: white !important;
            text-decoration: none;
        }


        .logo-text .orange {
            color: var(--accent-orange);
        }


        @media (max-width: 768px) {
            .container {
                padding: 1rem;
            }

            .card {
                margin: 1rem 0;
            }

            .form-control {
                padding: 12px 18px;
            }

            .btn-primary {
                padding: 12px 25px;
            }
        }
    </style>

    {% block stylesheets %}{% endblock %}

</head>
<body>
<header>
    <header>
        <div class="container">
            <div class="header-container">
                <div class="logo">
                    <div class="logo-icon">
                        <div class="fridge">
                            <div class="fridge-handle"></div>
                        </div>
                        <div class="orange-tag"></div>
                    </div>
                    <div>
                        <div class="logo-text">
                            <span class="green">COMPOSE </span><span class="orange">& ENJOY</span>
                        </div>
                        <div class="tagline">Transformez vos ingrédients</div>
                    </div>
                </div>
                <nav>
                    <ul>
                        <li><a href="#features">Comment ça marche</a></li>
                        <li><a href="#search">Rechercher</a></li>
                        <li><a href="#recipes">Recettes</a></li>
                        <li><a href="{{ path('app_login') }}" class="btn" style="padding: 8px 16px; font-size: 0.9rem;">Se connecter</a></li>
                        <!-- Dans votre navbar -->
                        <li>
                            <a class="pantry-link" href="{{ path('pantry_index') }}">
                                <i class="fas fa-archive"></i> Mon garde-manger
                            </a>
                        </li>
                    </ul>
                </nav>
            </div>
        </div>

    </header>
<main>
    {% block body %}{% endblock %}
</main>
{# Bootstrap JS #}
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>

{% block javascripts %}
    <script>
        // Toggle between login and register forms
        function toggleAuthMode() {
            const container = document.querySelector('.auth-container');
            container.classList.toggle('register-mode');
        }

        // Add smooth transitions
        document.addEventListener('DOMContentLoaded', function() {
            const forms = document.querySelectorAll('form');
            forms.forEach(form => {
                form.addEventListener('submit', function() {
                    const submitBtn = form.querySelector('.btn');
                    if (submitBtn) {
                        submitBtn.innerHTML = '<span>Chargement...</span>';
                        submitBtn.disabled = true;
                    }
                });
            });
        });
    </script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</header>
</body>
</html>

{% endblock %}


























































































