{% extends 'base.html.twig' %}

{% block title %}Recherche de recettes{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="row">
            <div class="col-md-4">
                <div class="card">
                    <div class="card-header">
                        <h5 class="mb-0">
                            <i class="fas fa-search"></i> Rechercher par ingrédients
                        </h5>
                    </div>
                    <div class="card-body">
                        <form method="post" action="{{ path('app_recette_search') }}">
                            <div class="mb-3">
                                <label class="form-label">Sélectionnez vos ingrédients disponibles :</label>
                                <div class="ingredient-list" style="max-height: 300px; overflow-y: auto;">
                                    {% for ingredient in ingredients %}
                                        <div class="form-check">
                                            <input class="form-check-input" type="checkbox"
                                                   name="ingredients[]" value="{{ ingredient.id }}"
                                                   id="ingredient_{{ ingredient.id }}"
                                                   {% if selectedIngredients is defined and ingredient in selectedIngredients %}checked{% endif %}>
                                            <label class="form-check-label" for="ingredient_{{ ingredient.id }}">
                                                {{ ingredient.nom|title }}
                                                {% if ingredient.categorie %}
                                                    <small class="text-muted">({{ ingredient.categorie }})</small>
                                                {% endif %}
                                            </label>
                                        </div>
                                    {% endfor %}
                                </div>
                            </div>

                            <button type="submit" class="btn btn-primary w-100">
                                <i class="fas fa-search"></i> Rechercher
                            </button>
                        </form>
                    </div>
                </div>

                {% if selectedIngredients is defined and selectedIngredients|length > 0 %}
                    <div class="card mt-3">
                        <div class="card-header">
                            <h6 class="mb-0">Ingrédients sélectionnés</h6>
                        </div>
                        <div class="card-body">
                            {% for ingredient in selectedIngredients %}
                                <span class="badge bg-primary me-1 mb-1">{{ ingredient.nom|title }}</span>
                            {% endfor %}
                        </div>
                    </div>
                {% endif %}
            </div>
            <div class="col-md-8">
                {% if searchPerformed is defined and searchPerformed %}
                    {% if recettes is defined and recettes|length > 0 %}
                        <div class="search-results">
                            <div class="results-header mb-4">
                                <div class="d-flex align-items-center justify-content-between">
                                    <h4 class="text-primary mb-0">
                                        <i class="fas fa-search me-2"></i>
                                        Résultats de recherche
                                    </h4>
                                    <span class="badge bg-primary fs-6">
                            {{ recettes|length }} recette{{ recettes|length > 1 ? 's' : '' }} trouvée{{ recettes|length > 1 ? 's' : '' }}
                        </span>
                                </div>
                                <hr class="mt-3">
                            </div>
                            <div class="recipes-grid">
                                {% for recette in recettes.recettes %}
                                    <div class="recipe-item mb-4">
                                        <div class="card shadow-sm border-0">
                                            <div class="card-body p-4">
                                                <div class="row align-items-center">
                                                    <div class="col-md-8">
                                                        <div class="recipe-info">
                                                            <h5 class="recipe-title mb-2">
                                                                <a href="{{ path('app_recette_show', {id: recette.id}) }}" class="text-decoration-none text-dark fw-bold">
                                                                    {{ recette.nom }}
                                                                </a>
                                                            </h5>
                                                            {% if recette.instructions %}
                                                                <p class="text-muted mb-2">
                                                                    {{ recette.instructions|slice(0, 120) }}{% if recette.instructions|length > 120 %}...{% endif %}
                                                                </p>
                                                            {% endif %}
                                                            <div class="recipe-meta d-flex flex-wrap gap-3">
                                                                {% if recette.tempsPreparation %}
                                                                    <small class="text-muted">
                                                                        <i class="fas fa-clock me-1"></i>
                                                                        {{ recette.tempsPreparation }} min
                                                                    </small>
                                                                {% endif %}
                                                                {% if recette.nombre_de_portion %}
                                                                    <small class="text-muted">
                                                                        <i class="fas fa-users me-1"></i>
                                                                        {{ recette.nombre_de_portion }} portions
                                                                    </small>
                                                                {% endif %}
                                                            </div>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                {% endfor %}
                    {% else %}
                        <div class="no-results text-center py-5">
                            <div class="card border-0">
                                <div class="card-body">
                                    <i class="fas fa-search fa-3x text-muted mb-3"></i>
                                    <h4 class="text-muted mb-3">Aucune recette trouvée</h4>
                                    <p class="text-muted mb-4">
                                        Désolé, nous n'avons trouvé aucune recette correspondant à vos critères de recherche.
                                    </p>
                                    <div class="suggestions">
                                        <h6 class="text-muted mb-2">Suggestions :</h6>
                                        <ul class="list-unstyled text-muted">
                                            <li>• Vérifiez l'orthographe de vos mots-clés</li>
                                            <li>• Essayez des termes plus généraux</li>
                                            <li>• Utilisez moins de filtres</li>
                                        </ul>
                                    </div>
                                    <a href="{{ path('app_recette_index') }}" class="btn btn-primary mt-3">
                                        <i class="fas fa-utensils me-2"></i>
                                        Voir toutes les recettes
                                    </a>
                                </div>
                            </div>
                        </div>
                    {% endif %}
                {% else %}
                    <div class="search-intro text-center py-5">
                        <div class="card border-0 bg-light">
                            <div class="card-body">
                                <i class="fas fa-utensils fa-3x text-primary mb-3"></i>
                                <h4 class="text-primary mb-3">Trouvez votre recette parfaite</h4>
                                <p class="text-muted mb-0">
                                    Utilisez les filtres de recherche pour découvrir des recettes adaptées à vos goûts et contraintes.
                                </p>
                            </div>
                        </div>
                    </div>
                {% endif %}
{% endblock %}
